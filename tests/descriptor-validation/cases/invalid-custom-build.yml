# invalid-custom-build: Custom 节点不应有 node 级 build（在 custom 内定义）
# 预期: dora validate 报错，包含 "not supported" 和 "build"
nodes:
  - id: c
    custom:
      path: node.py
      source: Local
    build: cargo build
