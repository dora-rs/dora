# invalid-runtime-build: Runtime 节点不应有 build 字段
# 预期: dora validate 报错，包含 "not supported" 和 "build"
nodes:
  - id: rt
    operators:
      - id: op
        shared-library: lib.so
    build: cargo build
