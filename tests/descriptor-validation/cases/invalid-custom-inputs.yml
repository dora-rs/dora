# invalid-custom-inputs: Custom 节点的 inputs 应写在 custom: 内部
# 预期: validate 报错，包含 "not supported" 和 "inputs"
nodes:
  - id: c
    custom:
      path: node.py
      source: Local
    inputs:
      data_in: ticker/tick
