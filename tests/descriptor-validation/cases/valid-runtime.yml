# valid-runtime: Runtime 节点合法配置
# 预期: dora validate 通过
nodes:
  - id: rt
    env:
      GLOBAL: "1"
    cpu_affinity: [0, 1]
    operators:
      - id: op
        shared-library: lib.so
        inputs:
          data: src/out
        outputs:
          - result
