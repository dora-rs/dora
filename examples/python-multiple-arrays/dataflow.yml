nodes:
  - id: sender
    build: pip install numpy pyarrow
    path: sender.py
    outputs:
      - multi_array_msg
    inputs:
      tick: adora/timer/millis/1000

  - id: receiver
    build: pip install numpy pyarrow
    path: receiver.py
    inputs:
      multi_array_msg: sender/multi_array_msg
