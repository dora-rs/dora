nodes:
  - id: control
    custom:
      source: control_node.py
      outputs:
        - direction

  - id: bridge
    custom:
      source: turtle_bridge.py
      inputs:
        direction: control/direction
      envs:
        ROS_HOST: localhost
        ROS_PORT: 9090
