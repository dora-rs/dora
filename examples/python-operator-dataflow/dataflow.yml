nodes:
  - id: webcam
    operator:
      build: pip install -r requirements.txt
      python: webcam.py
      inputs:
        tick: dora/timer/millis/50
      outputs:
        - image

  - id: object_detection
    operator:
      build: pip install -r requirements.txt
      python: object_detection.py
      send_stdout_as: stdout
      inputs:
        image: webcam/image
      outputs:
        - bbox
        - latency # Added for Project #1 Performance Tooling
        - stdout

  - id: plot
    operator:
      build: pip install -r requirements.txt
      python: plot.py
      inputs:
        image: webcam/image
        bbox: object_detection/bbox
        latency: object_detection/latency # Connected the new metric
        assistant_message: object_detection/stdout