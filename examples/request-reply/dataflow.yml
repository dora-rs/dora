nodes:
  - id: service-node
    path: service_node.py
    services:
      compute:
        type: server

  - id: requester-node
    path: requester_node.py
    inputs:
      tick: dora/timer/millis/200
    services:
      compute:
        type: client
        server: service-node/compute