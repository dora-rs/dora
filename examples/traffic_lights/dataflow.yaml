source:
  - metadata:
      name: my-source-1
    spec:
      run: python source.py
      selector:
        labels: my-source
    # or IP
        ip: 
          - 192.0.0.1

operator:
  - metadata:
      name: my-operator
      labels: traffic_light
    spec:
      run: python operator.py
      start: False
      input:
        selector:
          labels: my-source
      output:
        labels: traffic_light
      # env: 
         # - STATE: ... 

sink:
  - metadata:
      name: sink
      labels: sink-visualisation
    spec:
      run: python sink.py
      input:
        selector:
          labels: traffic_light
      
