[package]
name = "tui-interface"
version = "0.0.1"
edition = "2024"
rust-version = "1.85.0"
license = "Apache-2.0"
description = "Shared data contracts and service traits between Dora core and the TUI (see ADR-001)."
publish = false

[dependencies]
thiserror = "1.0"
