# CircleCI CI configuration for dora-rs/dora
#
# Mirrors the GitHub Actions workflow (.github/workflows/ci.yml) with
# CircleCI-native improvements:
#   - Workspaces share the CLI binary across downstream jobs (no rebuild)
#   - Fast gates and core tests run in parallel from the start
#   - Heavy runtime jobs (ROS2 bridge e2e, Windows runners) do NOT run on
#     PRs or merges -- they live in the nightly workflow; PR CI only compiles
#     them. Only the release-tag `examples-windows-heavy` job runs Windows here.
#   - Cargo registry cached across runs; compilation is per-job


version: 2.1

orbs:
  win: circleci/windows@5.0

# ────────────────────────────────────────────────────────────────────
# Reusable commands
# ────────────────────────────────────────────────────────────────────
commands:
  install-rust:
    description: Install Rust via rustup (for non-Docker executors)
    parameters:
      version:
        type: string
        default: "1.92.0"
      components:
        type: string
        default: ""
      targets:
        type: string
        default: ""
    steps:
      - run:
          name: Install Rust << parameters.version >>
          command: |
            if ! command -v rustup &>/dev/null; then
              curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs \
                | sh -s -- -y --default-toolchain << parameters.version >>
              echo 'export PATH="$HOME/.cargo/bin:$PATH"' >> "$BASH_ENV"
              source "$BASH_ENV"
            else
              rustup default << parameters.version >>
            fi
            if [ -n "<< parameters.components >>" ]; then
              rustup component add << parameters.components >>
            fi
            if [ -n "<< parameters.targets >>" ]; then
              rustup target add << parameters.targets >>
            fi

  install-rust-windows:
    description: Install Rust on Windows via rustup-init
    parameters:
      version:
        type: string
        default: "1.92.0"
    steps:
      - run:
          name: Install Rust << parameters.version >>
          shell: powershell.exe
          command: |
            if (-not (Get-Command rustup -ErrorAction SilentlyContinue)) {
              Invoke-WebRequest -Uri https://win.rustup.rs/x86_64 -OutFile rustup-init.exe
              .\rustup-init.exe -y --default-toolchain << parameters.version >> --default-host x86_64-pc-windows-msvc
              $env:PATH = "$env:USERPROFILE\.cargo\bin;$env:PATH"
              [Environment]::SetEnvironmentVariable("PATH", $env:PATH, "User")
            } else {
              rustup default << parameters.version >>
            }
      # Persist cargo PATH for subsequent bash.exe steps via BASH_ENV
      - run:
          name: Set Rust PATH for bash steps
          shell: bash.exe
          command: echo 'export PATH="$USERPROFILE/.cargo/bin:$PATH"' >> "$BASH_ENV"

  cargo-cache-restore:
    parameters:
      prefix:
        type: string
        default: cargo
    steps:
      - restore_cache:
          keys:
            - << parameters.prefix >>-v1-{{ arch }}-{{ checksum "Cargo.lock" }}
            - << parameters.prefix >>-v1-{{ arch }}-

  cargo-cache-save:
    parameters:
      prefix:
        type: string
        default: cargo
    steps:
      - save_cache:
          key: << parameters.prefix >>-v1-{{ arch }}-{{ checksum "Cargo.lock" }}
          paths:
            - ~/.cargo/registry/index
            - ~/.cargo/registry/cache
            - ~/.cargo/git/db

  setup-python-uv:
    description: Install Python 3.12 + uv + dora-node-api-python into a venv
    steps:
      - run:
          name: Install uv
          command: |
            curl -LsSf https://astral.sh/uv/install.sh | sh
            echo 'export PATH="$HOME/.local/bin:$PATH"' >> "$BASH_ENV"
            source "$BASH_ENV"
      - run:
          name: Create Python venv and install packages
          # Building dora-rs (PyO3) from source takes >10m with no output.
          no_output_timeout: 30m
          command: |
            uv venv --seed -p 3.12
            echo "export VIRTUAL_ENV=$PWD/.venv" >> "$BASH_ENV"
            echo "export PATH=$PWD/.venv/bin:\$PATH" >> "$BASH_ENV"
            source "$BASH_ENV"
            uv pip install pyarrow
            uv pip install -e apis/python/node
            uv pip install ruff pytest
            uv run python -c "import dora; print(dora.__file__)"

# ────────────────────────────────────────────────────────────────────
# Executors
# ────────────────────────────────────────────────────────────────────
executors:
  # Heavy compilation (tests, examples, CLI)
  # Note: upgrade to xlarge if your plan supports it for faster builds.
  linux:
    docker:
      - image: cimg/rust:1.92.0
    resource_class: large
    environment:
      CARGO_TERM_COLOR: always

  # Moderate compilation (clippy, bench, cross-check)
  linux-large:
    docker:
      - image: cimg/rust:1.92.0
    resource_class: large
    environment:
      CARGO_TERM_COLOR: always

  # Light compilation or tool-only jobs
  linux-medium:
    docker:
      - image: cimg/rust:1.92.0
    resource_class: medium
    environment:
      CARGO_TERM_COLOR: always

  # No-Rust jobs (typos, unwrap budget)
  linux-small:
    docker:
      - image: cimg/base:current
    resource_class: medium
    environment:
      CARGO_TERM_COLOR: always

  # Cross-compilation with `cross` (needs native Docker)
  linux-machine:
    machine:
      image: ubuntu-2204:current
    resource_class: large
    environment:
      CARGO_TERM_COLOR: always
      RUST_VERSION: "1.92.0"

# ────────────────────────────────────────────────────────────────────
# Jobs
# ────────────────────────────────────────────────────────────────────
jobs:

  # ===== Fast gates (no dependencies, run immediately) =====

  fmt:
    executor: linux-medium
    steps:
      - checkout
      - run: cargo fmt --all -- --check

  clippy:
    executor: linux-large
    environment:
      # The workspace pins `abi3-py311` in pyo3 features. With the
      # cimg/rust image's Python sitting at < 3.11, pyo3-build-config
      # 0.28+ aborts the interpreter probe. Disable the probe; the
      # `abi3-py311` feature on pyo3-build-config (Cargo.toml:128)
      # carries the ABI metadata without needing a live interpreter.
      # Mirrors the GHA nightly env at .github/workflows/nightly.yml:1335.
      PYO3_NO_PYTHON: "1"
    steps:
      - checkout
      - cargo-cache-restore:
          prefix: clippy
      - run:
          name: Clippy (warnings = errors)
          # dora-cli-api-python is a leaf cdylib that links libpython
          # via pyo3-ffi — incompatible with the job's `PYO3_NO_PYTHON=1`.
          # Exclude it so cargo doesn't try to link the cdylib at all.
          # Same exclusion shape used by every other CircleCI cargo
          # invocation (test-linux, cross-check-*, etc.).
          command: >
            cargo clippy --all
            --exclude dora-node-api-python
            --exclude dora-operator-api-python
            --exclude dora-ros2-bridge-python
            --exclude dora-cli-api-python
            -- -D warnings
      - cargo-cache-save:
          prefix: clippy

  typos:
    executor: linux-small
    steps:
      - checkout
      - run:
          name: Install typos-cli
          command: |
            # Asset names include the version (e.g. typos-v1.45.1-...), so
            # resolve the latest tag first via the GitHub /releases/latest redirect.
            VERSION=$(basename "$(curl -sSfL -o /dev/null -w '%{url_effective}' \
              https://github.com/crate-ci/typos/releases/latest)")
            mkdir -p /tmp/typos-dl
            curl -sSfL \
              "https://github.com/crate-ci/typos/releases/download/${VERSION}/typos-${VERSION}-x86_64-unknown-linux-musl.tar.gz" \
              | tar xz -C /tmp/typos-dl
            sudo install /tmp/typos-dl/typos /usr/local/bin/
      - run: typos

  audit:
    executor: linux-medium
    steps:
      - checkout
      - restore_cache:
          keys:
            - audit-tools-v1
      - run:
          name: Install cargo-audit + cargo-deny
          command: |
            command -v cargo-audit >/dev/null || cargo install cargo-audit
            command -v cargo-deny  >/dev/null || cargo install cargo-deny
      - save_cache:
          key: audit-tools-v1
          paths:
            - ~/.cargo/bin/cargo-audit
            - ~/.cargo/bin/cargo-deny
      - run: make qa-audit

  unwrap-budget:
    executor: linux-small
    steps:
      - checkout
      - run:
          name: Install ripgrep
          command: sudo apt-get update && sudo apt-get install -y ripgrep
      - run: make qa-unwrap

  check-license:
    executor: linux-medium
    steps:
      - checkout
      - cargo-cache-restore:
          prefix: license
      - restore_cache:
          keys:
            - lichking-v1
      - run:
          name: Install cargo-lichking
          command: command -v cargo-lichking >/dev/null || cargo install cargo-lichking
      - save_cache:
          key: lichking-v1
          paths:
            - ~/.cargo/bin/cargo-lichking
      - run: cargo lichking check
      - cargo-cache-save:
          prefix: license

  # ===== Core tests =====

  test-linux:
    executor: linux
    environment:
      # Skip pyo3-build-config's interpreter probe (image Python < 3.11).
      # Workspace pins `abi3-py311` on pyo3-build-config (Cargo.toml:128).
      PYO3_NO_PYTHON: "1"
    steps:
      - checkout
      - cargo-cache-restore
      - run:
          name: Test (excluding Python)
          # `cargo test --all` compiles everything, so separate
          # `cargo check` and `cargo build` steps are unnecessary.
          command: >
            cargo test --all
            --exclude dora-node-api-python
            --exclude dora-operator-api-python
            --exclude dora-ros2-bridge-python
            --exclude dora-cli-api-python
            --exclude dora-examples
          no_output_timeout: 30m
      - run:
          name: Check examples compile
          # [[example]] targets are not built by `cargo test --all`
          command: cargo check --examples

      # ── Tier 0 CLI smoke gates ──
      - run:
          name: CLI smoke -- argparse & help text
          command: |
            for sub in run up down build start stop restart list logs top \
                       topic node param record replay trace doctor status \
                       new graph expand validate; do
              cargo run --quiet -p dora-cli -- "$sub" --help > /dev/null
            done
            cargo run --quiet -p dora-cli -- --help > /dev/null
      - run:
          name: CLI smoke -- dora validate
          command: |
            cargo run --quiet -p dora-cli -- validate examples/rust-dataflow/dataflow.yml
            cargo run --quiet -p dora-cli -- validate examples/multiple-daemons/dataflow.yml
      - run:
          name: CLI semantic -- dora validate --strict-types
          command: |
            cargo run --quiet -p dora-cli -- validate --strict-types examples/typed-dataflow/dataflow.yml
            if cargo run --quiet -p dora-cli -- validate --strict-types tests/fixtures/type-mismatch.yml; then
              echo "ERROR: --strict-types accepted type-mismatch.yml"
              exit 1
            fi
      - run:
          name: CLI smoke -- dora expand
          command: cargo run --quiet -p dora-cli -- expand examples/module-dataflow/dataflow.yml > /dev/null
      - run:
          name: CLI semantic -- dora expand flattens modules
          command: |
            out=$(cargo run --quiet -p dora-cli -- expand examples/module-dataflow/dataflow.yml)
            for id in sender pipeline.doubler pipeline.filter receiver; do
              echo "$out" | grep -q "^- id: $id" \
                || { echo "ERROR: expand output missing node id '$id'"; exit 1; }
            done
            if echo "$out" | grep -q '^modules:'; then
              echo "ERROR: expand output still contains 'modules:' block"
              exit 1
            fi
      - run:
          name: CLI smoke -- dora graph (mermaid)
          command: |
            cargo run --quiet -p dora-cli -- graph examples/rust-dataflow/dataflow.yml --mermaid > /dev/null
            cargo run --quiet -p dora-cli -- graph examples/multiple-daemons/dataflow.yml --mermaid > /dev/null
      - run:
          name: CLI semantic -- dora graph contains expected nodes
          command: |
            out=$(cargo run --quiet -p dora-cli -- graph examples/rust-dataflow/dataflow.yml --mermaid)
            echo "$out" | grep -q '^flowchart ' \
              || { echo "ERROR: graph output missing 'flowchart' header"; exit 1; }
            for id in rust-node rust-sink rust-status-node; do
              echo "$out" | grep -q "$id" \
                || { echo "ERROR: graph output missing node '$id'"; exit 1; }
            done

      - cargo-cache-save
      # Share CLI binary with downstream jobs via workspace
      - persist_to_workspace:
          root: .
          paths:
            - target/debug/dora

  test-macos:
    macos:
      xcode: "15.4"
    resource_class: macos.m1.medium.gen1
    environment:
      CARGO_TERM_COLOR: always
      # Same pyo3-build-config probe-skip as test-linux above.
      PYO3_NO_PYTHON: "1"
    steps:
      - checkout
      - install-rust:
          version: "1.92.0"
      - cargo-cache-restore:
          prefix: cargo-macos
      - run:
          name: Test (excluding Python)
          command: >
            cargo test --all
            --exclude dora-node-api-python
            --exclude dora-operator-api-python
            --exclude dora-ros2-bridge-python
            --exclude dora-cli-api-python
            --exclude dora-examples
          no_output_timeout: 30m
      - run:
          name: Check examples compile
          command: cargo check --examples
      - cargo-cache-save:
          prefix: cargo-macos

  # ===== Examples (cross-platform) =====

  examples-linux:
    executor: linux
    environment:
      # Same pyo3-build-config probe-skip as test-linux above.
      PYO3_NO_PYTHON: "1"
    steps:
      - checkout
      - cargo-cache-restore
      - run:
          name: Install C/C++ build tools
          command: sudo apt-get update && sudo apt-get install -y clang cmake
      - run:
          name: Build examples + CLI binary
          command: |
            cargo build --examples
            cargo build -p dora-cli
            echo 'export PATH="'"$PWD"'/target/debug:$PATH"' >> "$BASH_ENV"
            source "$BASH_ENV"
          no_output_timeout: 30m
      - run:
          name: Rust Dataflow example
          command: cargo run --example rust-dataflow
          no_output_timeout: 30m
      - run:
          name: Rust Git Dataflow example
          command: cargo run --example rust-dataflow-git
          no_output_timeout: 30m
      - run:
          name: Multiple Daemons example
          command: cargo run --example multiple-daemons
          no_output_timeout: 30m
      - run:
          name: C Dataflow example
          command: cargo run --example c-dataflow
          no_output_timeout: 15m
      - run:
          name: C++ Dataflow example
          command: cargo run --example cxx-dataflow
          no_output_timeout: 15m
      - run:
          name: Install Arrow C++ library
          command: |
            sudo apt-get update
            sudo apt-get install -y -V ca-certificates lsb-release wget
            wget https://apache.jfrog.io/artifactory/arrow/$(lsb_release --id --short | tr 'A-Z' 'a-z')/apache-arrow-apt-source-latest-$(lsb_release --codename --short).deb
            sudo apt-get install -y -V ./apache-arrow-apt-source-latest-$(lsb_release --codename --short).deb
            sudo apt-get update
            sudo apt-get install -y -V libarrow-dev libarrow-glib-dev
      - run:
          name: C++ Arrow Dataflow example
          command: cargo run --example cxx-arrow-dataflow
          no_output_timeout: 15m
      - run:
          name: CMake example
          command: cargo run --example cmake-dataflow
          no_output_timeout: 30m
      - cargo-cache-save

  examples-macos:
    macos:
      xcode: "15.4"
    resource_class: macos.m1.medium.gen1
    environment:
      CARGO_TERM_COLOR: always
      # macOS Xcode 15.4 ships Python 3.9; `cargo build --examples` +
      # `cargo install dora-cli` both trigger dora-runtime's build.rs
      # which calls `pyo3_build_config::use_pyo3_cfgs()` unconditionally
      # (binaries/runtime/build.rs). Skip the interpreter probe.
      PYO3_NO_PYTHON: "1"
    steps:
      - checkout
      - install-rust:
          version: "1.92.0"
      - cargo-cache-restore:
          prefix: cargo-macos-examples
      - run:
          name: Build examples + CLI binary
          command: |
            cargo build --examples
            cargo build -p dora-cli
            echo 'export PATH="'"$PWD"'/target/debug:$PATH"' >> "$BASH_ENV"
            source "$BASH_ENV"
          no_output_timeout: 30m
      - run:
          name: Rust Dataflow example
          command: cargo run --example rust-dataflow
          no_output_timeout: 30m
      - run:
          name: Rust Git Dataflow example
          command: cargo run --example rust-dataflow-git
          no_output_timeout: 30m
      - run:
          name: Multiple Daemons example
          command: cargo run --example multiple-daemons
          no_output_timeout: 30m
      - run:
          name: C Dataflow example
          command: cargo run --example c-dataflow
          no_output_timeout: 15m
      # C++ examples skipped on macOS — Xcode 15.4's libc++ has a
      # template incompatibility with CXX bridge's rust::Slice::iterator
      # (std::pointer_traits_element_type). Tracked upstream in cxx.
      # The C++ examples are covered on Linux.
      - cargo-cache-save:
          prefix: cargo-macos-examples

  # examples-windows-heavy is the ONLY remaining Windows CircleCI job: it
  # builds + runs the Multiple-Daemons / C / C++ examples on Windows and runs
  # ONLY on release tags (see workflow filter). The per-PR/per-merge Windows
  # jobs (test-windows, examples-windows, cli-windows) were removed — Dora's
  # Windows user surface is narrow and the nightly workflow already tests +
  # runs examples on Windows daily.
  examples-windows-heavy:
    executor: win/default
    environment:
      CARGO_TERM_COLOR: always
      PYO3_NO_PYTHON: "1"
    steps:
      - checkout
      - install-rust-windows:
          version: "1.92.0"
      - cargo-cache-restore:
          prefix: cargo-win-examples-heavy
      - run:
          name: Build examples + CLI binary
          shell: bash.exe
          command: |
            cargo build --examples
            cargo build -p dora-cli
          no_output_timeout: 30m
      - run:
          name: Multiple Daemons example
          shell: bash.exe
          command: cargo run --example multiple-daemons
          no_output_timeout: 30m
      - run:
          name: C Dataflow example
          shell: bash.exe
          command: cargo run --example c-dataflow
          no_output_timeout: 15m
      - run:
          name: C++ Dataflow example
          shell: bash.exe
          command: cargo run --example cxx-dataflow
          no_output_timeout: 15m
      - cargo-cache-save:
          prefix: cargo-win-examples-heavy

  # ===== CLI integration tests =====
  # Split into Rust-only and Python sub-jobs for parallelism.

  cli-rust:
    executor: linux
    steps:
      - checkout
      - attach_workspace:
          at: .
      - cargo-cache-restore
      - run:
          name: Install CLI from workspace
          command: |
            cp target/debug/dora ~/.cargo/bin/dora
            chmod +x ~/.cargo/bin/dora
      - run:
          name: Test Rust template project
          command: |
            dora new test_rust_project --internal-create-with-path-dependencies
            cd test_rust_project
            cargo build --all
            dora run dataflow.yml --stop-after 10s
          no_output_timeout: 45m
      - run:
          name: Test Dynamic Rust Dataflow
          command: |
            dora up
            dora list
            dora build examples/rust-dataflow/dataflow_dynamic.yml
            dora start examples/rust-dataflow/dataflow_dynamic.yml --name ci-rust-dynamic --detach
            cargo run -p rust-dataflow-example-sink-dynamic
            sleep 5
            dora stop --name ci-rust-dynamic --grace-duration 5s
            dora destroy
          no_output_timeout: 45m
      - run:
          name: Error Event example
          command: |
            dora build examples/error-propagation/dataflow.yml
            output=$(dora run examples/error-propagation/dataflow.yml 2>&1 || true)
            echo "$output"
            echo "$output" | grep -q "Received error from node" \
              || { echo "ERROR: consumer did not receive NodeFailed event"; exit 1; }
          no_output_timeout: 10m
      - cargo-cache-save

  cli-python:
    executor: linux
    steps:
      - checkout
      - attach_workspace:
          at: .
      - cargo-cache-restore
      - run:
          name: Install CLI from workspace
          command: |
            cp target/debug/dora ~/.cargo/bin/dora
            chmod +x ~/.cargo/bin/dora
      - setup-python-uv
      - run:
          name: Install OpenCV system dependencies
          # opencv-python (pulled by dora-hub packages) needs X11 libs
          # that cimg/rust doesn't ship.
          command: |
            sudo apt-get update
            sudo apt-get install -y libxcb1 libgl1-mesa-glx libglib2.0-0 \
              libsm6 libxrender1 libxext6
      - run:
          name: Test Python template project
          command: |
            dora new test_python_project --lang python --internal-create-with-path-dependencies
            cd test_python_project
            dora build dataflow.yml --uv
            uv run ruff check .
            # Workspace pyproject.toml (generated by --internal-create-with-path-dependencies)
            # pins dora-rs to ../apis/python/node and lists all three nodes as workspace
            # members, so uv run pytest finds everything without explicit installs.
            uv run pytest
            export OPERATING_MODE=SAVE
            dora build dataflow.yml --uv
            dora run dataflow.yml --uv --stop-after 10s
          no_output_timeout: 80m
      - run:
          name: Test Python Node example
          command: |
            dora build examples/python-dataflow/dataflow.yml --uv
            dora run examples/python-dataflow/dataflow.yml --uv --stop-after 10s
          no_output_timeout: 20m
      - run:
          name: Test Python Dynamic Node example
          command: |
            dora build examples/python-dataflow/dataflow_dynamic.yml --uv
            uv pip install -e apis/python/node
            dora up
            dora start examples/python-dataflow/dataflow_dynamic.yml --name ci-python-dynamic --detach --uv
            uv run opencv-plot
            sleep 10
            dora stop --name ci-python-dynamic --grace-duration 30s
            dora destroy
          no_output_timeout: 20m
      - run:
          name: Test Python Operator example
          command: |
            dora build examples/python-operator-dataflow/dataflow.yml --uv
            uv pip install -e apis/python/node
            dora run examples/python-operator-dataflow/dataflow.yml --uv --stop-after 20s
          no_output_timeout: 20m
      - run:
          name: Test Python Multiple Arrays
          command: |
            dora build examples/python-multiple-arrays/dataflow.yml --uv
            dora run examples/python-multiple-arrays/dataflow.yml --uv --stop-after 30s
          no_output_timeout: 30m
      - run:
          name: Test Python Async example
          command: dora run examples/python-async/dataflow.yaml --uv --stop-after 5m
          no_output_timeout: 30m
      - run:
          name: Test Python Drain example
          environment:
            OTEL_SDK_DISABLED: "true"
          command: dora run examples/python-drain/dataflow.yaml --uv
          no_output_timeout: 30m
      - run:
          name: Test Python Dataflow Builder API (YAML generation)
          command: uv run --with PyYAML python examples/python-dataflow-builder/test_builder_api.py
          no_output_timeout: 5m
      - run:
          name: Python Queue Latency Test
          command: dora run tests/queue_size_latest_data_python/dataflow.yaml --uv
          no_output_timeout: 30m
      - run:
          name: Python Queue Latency + Timeout Test
          command: dora run tests/queue_size_and_timeout_python/dataflow.yaml --uv
          no_output_timeout: 30m
      - run:
          name: Rust Queue Latency Test
          command: |
            dora build tests/queue_size_latest_data_rust/dataflow.yaml --uv
            dora run tests/queue_size_latest_data_rust/dataflow.yaml --uv
          no_output_timeout: 30m
      - cargo-cache-save

  cli-templates:
    executor: linux
    steps:
      - checkout
      - attach_workspace:
          at: .
      - cargo-cache-restore
      - run:
          name: Install CLI from workspace
          command: |
            cp target/debug/dora ~/.cargo/bin/dora
            chmod +x ~/.cargo/bin/dora
      - run:
          name: Test C template project
          command: |
            dora new test_c_project --lang c --internal-create-with-path-dependencies
            cd test_c_project
            cmake -B build
            cmake --build build
            cmake --install build
            dora run dataflow.yml --stop-after 10s
          no_output_timeout: 45m
      - run:
          name: Test C++ template project
          command: |
            dora new test_cxx_project --lang cxx --internal-create-with-path-dependencies
            cd test_cxx_project
            cmake -B build
            cmake --build build
            cmake --install build
            dora run dataflow.yml --stop-after 10s
          no_output_timeout: 45m
      - cargo-cache-save

  cli-macos:
    macos:
      xcode: "15.4"
    resource_class: macos.m1.medium.gen1
    environment:
      CARGO_TERM_COLOR: always
      # `cargo install --path binaries/cli --locked` resolves to dora-
      # runtime as a transitive dep; runtime's build.rs runs pyo3-build-
      # config unconditionally. Skip the probe on the < 3.11 macOS image.
      PYO3_NO_PYTHON: "1"
    steps:
      - checkout
      - install-rust:
          version: "1.92.0"
      - cargo-cache-restore:
          prefix: cargo-macos-cli
      - run:
          name: Build + install CLI
          command: cargo install --path binaries/cli --locked
          no_output_timeout: 45m
      - run:
          name: Test Rust template project
          command: |
            dora new test_rust_project --internal-create-with-path-dependencies
            cd test_rust_project
            cargo build --all
            dora run dataflow.yml --stop-after 10s
          no_output_timeout: 45m
      - run:
          name: Test Dynamic Rust Dataflow
          command: |
            dora up
            dora list
            dora build examples/rust-dataflow/dataflow_dynamic.yml
            dora start examples/rust-dataflow/dataflow_dynamic.yml --name ci-rust-dynamic --detach
            cargo run -p rust-dataflow-example-sink-dynamic
            sleep 5
            dora stop --name ci-rust-dynamic --grace-duration 5s
            dora destroy
          no_output_timeout: 45m
      - run:
          name: Error Event example
          command: |
            dora build examples/error-propagation/dataflow.yml
            output=$(dora run examples/error-propagation/dataflow.yml 2>&1 || true)
            echo "$output"
            echo "$output" | grep -q "Received error from node" \
              || { echo "ERROR: consumer did not receive NodeFailed event"; exit 1; }
          no_output_timeout: 10m
      - cargo-cache-save:
          prefix: cargo-macos-cli

  # ===== Benchmark example (Linux-only) =====
  # Reduced from 3 platforms -- criterion regression job below is the
  # authoritative benchmark gate; this is a "does it run" check.
  bench-example:
    executor: linux
    environment:
      # Same pyo3-build-config probe-skip as test-linux above.
      PYO3_NO_PYTHON: "1"
    steps:
      - checkout
      - cargo-cache-restore:
          prefix: bench-example
      - run:
          name: Benchmark example (release)
          command: cargo run --example benchmark --release
          no_output_timeout: 45m
      - cargo-cache-save:
          prefix: bench-example

  # ===== E2E tests =====
  e2e:
    executor: linux
    environment:
      # Same pyo3-build-config probe-skip as test-linux above.
      PYO3_NO_PYTHON: "1"
    steps:
      - checkout
      - attach_workspace:
          at: .
      - cargo-cache-restore
      - run:
          name: Install CLI from workspace
          command: |
            sudo cp target/debug/dora /usr/local/bin/dora
            sudo chmod +x /usr/local/bin/dora
      - run:
          name: Run ws-cli-e2e tests
          command: cargo test --test ws-cli-e2e -- --test-threads=1
          no_output_timeout: 10m
      - run:
          name: Run fault-tolerance tests
          command: cargo test --test fault-tolerance-e2e -- --test-threads=1
          no_output_timeout: 15m
      # `dora node` lifecycle E2E (issue #1703). The shared test
      # binary lives under the `dora-examples` package which the
      # bulk `cargo test --all` step excludes. Filter to Rust + C++
      # variants here; the Python variant runs in `contract-tests`
      # below where uv is already installed.
      - run:
          name: Run dora node lifecycle E2E (Rust + C++)
          # Cargo's TESTNAME positional is single-valued; multiple
          # substring filters go after `--` for libtest.
          command: >
            cargo test -p dora-examples --test node-lifecycle-e2e
            --
            --test-threads=1
            lifecycle_rust lifecycle_cxx
          no_output_timeout: 15m
      - cargo-cache-save

  # ===== Semantic contract tests =====
  contract-tests:
    executor: linux
    environment:
      # Same pyo3-build-config probe-skip as test-linux above.
      PYO3_NO_PYTHON: "1"
    steps:
      - checkout
      - attach_workspace:
          at: .
      - cargo-cache-restore
      - run:
          name: Install CLI from workspace
          command: |
            cp target/debug/dora ~/.cargo/bin/dora
            chmod +x ~/.cargo/bin/dora
      - setup-python-uv
      - run:
          name: Pin uv to the CI venv for the contract examples
          # The contract examples spawn `uv run python` from nested example
          # dirs; pin uv to the venv that holds the editable dora package
          # rather than cwd-based discovery. Scoped to this job on purpose —
          # cli-python's template-project nodes need their own per-project env
          # resolution and break under a global UV_PYTHON pin.
          command: |
            echo "export UV_PYTHON=$PWD/.venv/bin/python" >> "$BASH_ENV"
            source "$BASH_ENV"
      - run:
          name: Run semantic contract tests
          # Wall-clock cap (dora-rs/dora#2064): a hung `--uv` dataflow keeps
          # emitting log output, so CircleCI's output-based `no_output_timeout`
          # never fires and the job runs ~50min–2h to the hard wall. `timeout`
          # fires regardless of output. With `--test-threads=1 --nocapture` the
          # last `test contract_… ` line without a following result names the
          # hung test. Normal runtime is well under 28m (full job ~22m).
          command: |
            timeout --kill-after=30s 1680s \
              cargo test -p dora-examples --test example-smoke contract_ \
              -- --test-threads=1 --nocapture
            rc=$?
            if [ "$rc" = 124 ] || [ "$rc" = 137 ]; then
              echo "ERROR (dora#2064): contract tests hit the 28m wall-clock cap."
              echo "The hung test is the last 'test contract_…' line above with no 'ok'/'FAILED' after it."
              exit 1
            fi
            exit $rc
          no_output_timeout: 30m
      # Python variant of the lifecycle test piggybacks on this
      # job because it needs uv (already set up via setup-python-uv
      # above). Rust + C++ variants run in the `e2e` job.
      - run:
          name: Run dora node lifecycle E2E (Python)
          # Wall-clock cap, same rationale as the contract step above (#2064).
          command: |
            timeout --kill-after=30s 840s \
              cargo test -p dora-examples --test node-lifecycle-e2e \
              -- --test-threads=1 lifecycle_python
            rc=$?
            if [ "$rc" = 124 ] || [ "$rc" = 137 ]; then
              echo "ERROR (dora#2064): lifecycle_python hit the 14m wall-clock cap (hung --uv dataflow)."
              exit 1
            fi
            exit $rc
          no_output_timeout: 15m
      - cargo-cache-save

  # ===== Benchmark regression check =====
  bench:
    executor: linux-large
    environment:
      # Same pyo3-build-config probe-skip as test-linux above.
      PYO3_NO_PYTHON: "1"
    steps:
      - checkout
      - cargo-cache-restore:
          prefix: bench
      - restore_cache:
          keys:
            - bench-baseline-v1-{{ arch }}-
      - run:
          name: Compile benchmarks
          command: |
            cargo bench -p dora-message --no-run
            cargo bench -p dora-daemon --features bench --no-run
      - run:
          name: Run benchmarks
          command: |
            cargo bench -p dora-message 2>/dev/null | tee bench-output.txt
            cargo bench -p dora-daemon --features bench 2>/dev/null | tee -a bench-output.txt
      - save_cache:
          key: bench-baseline-v1-{{ arch }}-{{ .Revision }}
          paths:
            - target/criterion
      - store_artifacts:
          path: bench-output.txt
          destination: benchmark-results.txt
      - cargo-cache-save:
          prefix: bench

  # ===== Cross-compilation checks =====

  cross-check-native:
    parameters:
      target:
        type: string
      resource:
        type: string
        default: large
      gcc:
        type: string
        default: ""
    docker:
      - image: cimg/rust:1.92.0
    resource_class: << parameters.resource >>
    environment:
      CARGO_TERM_COLOR: always
      # Mirror the GHA nightly env at nightly.yml:1335. cimg/rust:1.92.0
      # ships Python < 3.11, which fails pyo3-build-config 0.28+'s
      # interpreter-vs-abi3-minimum check. abi3 metadata is already
      # baked in via the workspace `abi3-py311` feature.
      PYO3_NO_PYTHON: "1"
    steps:
      - checkout
      - cargo-cache-restore:
          prefix: cross-<< parameters.target >>
      - when:
          condition: << parameters.gcc >>
          steps:
            - run:
                name: Install cross-compilation toolchain
                command: sudo apt-get update && sudo apt-get install -y << parameters.gcc >>
      - run:
          name: Install target << parameters.target >>
          command: rustup target add << parameters.target >>
      - run:
          name: Cargo check --target << parameters.target >>
          command: >
            cargo check --target << parameters.target >> --all
            --exclude dora-node-api-python
            --exclude dora-operator-api-python
            --exclude dora-ros2-bridge-python
            --exclude dora-cli-api-python
      - cargo-cache-save:
          prefix: cross-<< parameters.target >>

  cross-check-macos:
    parameters:
      target:
        type: string
    macos:
      xcode: "15.4"
    resource_class: macos.m1.medium.gen1
    environment:
      CARGO_TERM_COLOR: always
      # System Python on this macOS image is < 3.11. Same reasoning as
      # cross-check-native above.
      PYO3_NO_PYTHON: "1"
    steps:
      - checkout
      - install-rust:
          version: "1.92.0"
          targets: << parameters.target >>
      - cargo-cache-restore:
          prefix: cross-macos-<< parameters.target >>
      - run:
          name: Cargo check --target << parameters.target >>
          command: >
            cargo check --target << parameters.target >> --all
            --exclude dora-node-api-python
            --exclude dora-operator-api-python
            --exclude dora-ros2-bridge-python
            --exclude dora-cli-api-python
      - cargo-cache-save:
          prefix: cross-macos-<< parameters.target >>

  cross-check-cross:
    parameters:
      target:
        type: string
    machine:
      image: ubuntu-2204:current
    resource_class: large
    environment:
      CARGO_TERM_COLOR: always
      RUST_VERSION: "1.92.0"
      # cross-rs Docker images ship Python 3.10, below our abi3-py311
      # minimum. Disable the interpreter probe (same as the GHA nightly
      # at nightly.yml:1335) AND pass the env through to the container
      # `cross` spawns — `CROSS_BUILD_ENV_PASSTHROUGH` maps to
      # `build.env.passthrough` in Cross.toml.
      PYO3_NO_PYTHON: "1"
      CROSS_BUILD_ENV_PASSTHROUGH: "PYO3_NO_PYTHON"
    steps:
      - checkout
      - install-rust:
          version: "1.92.0"
      - cargo-cache-restore:
          prefix: cross-<< parameters.target >>
      - run:
          name: Install cross
          command: cargo install cross --locked
      - run:
          name: Cross check --target << parameters.target >>
          command: >
            cross check --target << parameters.target >> --all
            --exclude dora-node-api-python
            --exclude dora-operator-api-python
            --exclude dora-ros2-bridge-python
            --exclude dora-cli-api-python
      - cargo-cache-save:
          prefix: cross-<< parameters.target >>

  # ===== MSRV check =====
  msrv:
    executor: linux-large
    environment:
      # `cargo hack check --workspace` builds the whole workspace,
      # which triggers pyo3-build-config 0.28+'s interpreter probe.
      # Same fix as the cross-check + clippy jobs above.
      PYO3_NO_PYTHON: "1"
    steps:
      - checkout
      - cargo-cache-restore:
          prefix: msrv
      - restore_cache:
          keys:
            - hack-v1
      - run:
          name: Install cargo-hack
          command: |
            command -v cargo-hack >/dev/null || cargo install cargo-hack
      - save_cache:
          key: hack-v1
          paths:
            - ~/.cargo/bin/cargo-hack
      - run:
          name: MSRV check
          # Same `--exclude` set as the test-linux job above. With
          # `PYO3_NO_PYTHON=1` set, pyo3-build-config 0.28+ emits a
          # config with `suppress_build_script_link_lines=false` and
          # no `lib_name`, so any crate that depends on pyo3 fails to
          # link with "attempted to link to Python shared library but
          # config does not contain lib_name". Excluding the three
          # python-binding crates matches the test-linux strategy and
          # restores green on this check.
          command: >
            cargo hack check --rust-version --workspace --ignore-private --locked
            --exclude dora-node-api-python
            --exclude dora-operator-api-python
            --exclude dora-ros2-bridge-python
      - cargo-cache-save:
          prefix: msrv

# ────────────────────────────────────────────────────────────────────
# Workflows
# ────────────────────────────────────────────────────────────────────
workflows:
  ci:
    jobs:
      # ── Fast gates (immediate, no dependencies) ──
      - fmt
      - clippy
      - typos
      - audit
      - unwrap-budget
      - check-license

      # ── Core tests (immediate, no dependencies) ──
      # Unlike the GitHub Actions config these do NOT wait for fast gates.
      # Each job fails on its own if there's a compilation error.
      - test-linux
      - test-macos

      # ── Linux integration (require test-linux for workspace artifact) ──
      - e2e:
          requires: [test-linux]
      - contract-tests:
          requires: [test-linux]
      - cli-rust:
          requires: [test-linux]
      - cli-python:
          requires: [test-linux]
      - cli-templates:
          requires: [test-linux]

      # ── Linux integration (no dependency -- uses cargo cache only) ──
      - examples-linux
      - bench-example
      - bench
      - msrv

      # ── macOS integration ──
      - examples-macos
      - cli-macos

      # ── Windows integration (release-tag only) ──
      # Per-PR/per-merge Windows CI was removed (narrow user surface; the
      # nightly workflow covers Windows). Only examples-windows-heavy remains,
      # gated to release tags as a pre-release confidence check.
      - examples-windows-heavy:
          filters:
            tags:
              only: /^v[0-9]+\.[0-9]+.*/
            branches:
              ignore: /.*/

      # ── Cross-compilation ──
      - cross-check-native:
          name: cross-check-x86_64-linux-gnu
          target: x86_64-unknown-linux-gnu
      - cross-check-native:
          name: cross-check-x86_64-windows-gnu
          target: x86_64-pc-windows-gnu
          gcc: "gcc-mingw-w64-x86-64 g++-mingw-w64-x86-64"
      - cross-check-cross:
          name: cross-check-i686-linux-gnu
          target: i686-unknown-linux-gnu
      - cross-check-cross:
          name: cross-check-aarch64-linux-gnu
          target: aarch64-unknown-linux-gnu
      - cross-check-cross:
          name: cross-check-aarch64-linux-musl
          target: aarch64-unknown-linux-musl
      - cross-check-cross:
          name: cross-check-armv7-linux-musleabihf
          target: armv7-unknown-linux-musleabihf
      - cross-check-macos:
          name: cross-check-aarch64-apple-darwin
          target: aarch64-apple-darwin
      - cross-check-macos:
          name: cross-check-x86_64-apple-darwin
          target: x86_64-apple-darwin
